/*jslint nomen: true */
/*globals define: true */

define([
],

function() {
  'use strict';

  var settings = {
    api: {
      baseurl: 'https://data.detroitledger.org/api/1.0',
      dateFormat: 'ddd, DD MMM YYYY HH:mm:ss ZZ'
    }
  };

  return settings;
});
